[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Official Next.js runtime for Netlify. Handles SSR, API routes
# (contact form, Keystatic) and image optimization automatically.
[[plugins]]
  package = "@netlify/plugin-nextjs"
